const ALL = 'all';
const NOT_FRIENDS = 'not-friends';

const phoneBookFilter = [
    {
        label: 'contacts.phone-book.all',
        value: ALL,
    },
    {
        label: 'contacts.phone-book.not-friends',
        value: NOT_FRIENDS,
    },
];

export default phoneBookFilter;
export { ALL, NOT_FRIENDS };
