const routes = {
    chats: '/',
    profile: '/profile',
    feed: '/feed',
    contacts: '/contacts',
    settings: '/settings',
    logOut: '/log-out',
    register: '/register',
    404: '*',
};

export default routes;
