const routes = {
    chats: '/',
    profile: '/profile',
    groups: '/groups',
    contacts: '/contacts',
    settings: '/settings',
    logOut: '/log-out',
    register: '/register',
};

export default routes;
