const routes = {
    chats: '/',
    profile: '/profile',
    groups: '/groups',
    contacts: '/contacts',
    settings: '/settings',
    logOut: '/log-out',
    register: '/register',
    signIn: '/sign-in',
};

export default routes;
