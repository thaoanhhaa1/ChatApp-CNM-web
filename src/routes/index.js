import { Home, Register } from '~/pages';

const routes = [
    {
        path: '/',
        component: Home,
    },

    {
        path: '/register',
        component: Register,
    },
];

export default routes;
